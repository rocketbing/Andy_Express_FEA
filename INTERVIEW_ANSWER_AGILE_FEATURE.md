# 面试回答：Sprint 末期紧急添加 Feature

## 简短版本（1-2分钟）

**问题**: "请分享一个你在项目中使用敏捷方法论处理紧急需求的经历"

**回答**:

在我们Andy Express项目的Sprint末期，距离交付还有1天的时候，业务部门突然要求添加一个功能：在订单流水、退货流水和取消订单三个模块中，添加按日期范围导出Excel的功能，因为财务部门急需这个功能进行月度对账。

当时我们面临的问题是时间非常紧迫，而且这个功能涉及到3个不同的模块，范围比较大。我的做法是：

首先，我立即和团队以及业务方开了个紧急会议，快速澄清了需求细节，确认了优先级。然后，我快速分解了任务并评估了工作量。最关键的是，我发现这个功能虽然涉及3个模块，但其实逻辑很相似，可以复用组件。所以我提出了一个方案：创建一个可复用的日期范围选择组件，然后三个模块都可以用这个组件。

在具体执行上，我负责创建可复用的日期选择组件，以及集成第一个模块（订单流水），我的同事负责其他两个模块。这样做的好处是，一旦第一个模块完成了，后面的模块就可以快速复制，大大提高了效率。

同时，我也提前和后端沟通了API接口，确认了可以在现有API上添加日期参数，避免了后续的阻塞。

最终，我们按时完成了这个功能，而且代码质量也保持得不错，因为通过组件复用，减少了重复代码。业务方对结果也很满意，财务部门可以立即使用这个功能进行对账。

这个经历让我深刻体会到，在紧急情况下，代码复用和组件化思维真的很重要，关键时刻能大幅提升开发效率。还有就是提前沟通和协调的重要性，避免在最后时刻遇到阻塞。

---

## 详细版本（可作为扩展回答）

**问题**: "请详细描述一下这个经历，你在其中发挥了什么作用？"

**回答**:

在我们Andy Express项目的Sprint末期，距离交付还有1天的时候，业务部门突然要求添加一个功能：在订单流水、退货流水和取消订单三个模块中，添加按日期范围导出Excel的功能，财务部门急需这个功能进行月度对账。

**我当时的角色和行动**:

作为前端开发负责人，我的第一个反应是，不能直接说时间不够，而是要快速评估这个需求的可行性和工作量。所以我立即和团队、Product Owner以及业务方开了个紧急会议，大概花了15分钟就澄清了需求细节，确认了这是P0级别的需求。

然后我快速进行了任务分解。我发现虽然涉及3个模块，但其实每个模块的需求很相似：都是需要日期选择器、调用API、然后导出Excel。所以我们系统已经有Excel导出的工具函数，我只需要创建一个可复用的日期范围选择组件，然后三个模块都可以复用这个组件。

在具体分工上，我负责：
1. 创建可复用的日期范围选择Modal组件（UI层）
2. 集成第一个模块（订单流水），包括API调用、数据过滤、数据转换等业务逻辑
3. 和后端协调API接口，确认可以添加日期参数

我的同事负责其他两个模块的集成。这样做的策略是，我先完成一个模块，验证可行性后，后面两个模块就可以快速复制，大大提高了效率。

**遇到的挑战和解决方案**:

第一个挑战是时间紧迫。我的解决方案是采用增量交付的方式，先完成一个模块，立即测试和演示，确认可行后再复制到其他模块。

第二个挑战是担心代码质量。因为我们已经有Excel导出的工具函数，所以我复用现有的代码模式，创建的可复用组件也遵循了项目的代码规范，降低了引入bug的风险。

第三个挑战是后端API的协调。我提前和后端沟通，确认了可以在现有API上添加日期参数。如果后端来不及改，我们也准备了备选方案：前端先做全量获取，然后在前端按日期范围过滤。

**最终结果**:

我们按时完成了这个功能，三个模块都支持按日期范围导出Excel。代码质量也保持得不错，因为通过组件复用，减少了重复代码，而且遵循了现有的代码规范。

业务方对结果很满意，财务部门可以立即使用这个功能进行月度对账。更重要的是，我们没有影响到其他Sprint任务的交付，整个Sprint还是按时完成了。

**我的收获**:

这个经历让我深刻体会到几个点：
1. 代码复用和组件化思维的重要性，关键时刻能大幅提升开发效率
2. 提前沟通和协调的重要性，避免在最后时刻遇到阻塞
3. 在紧急情况下，快速评估、分解任务、并行开发的能力
4. 虽然时间紧迫，但不能牺牲代码质量，通过复用和遵循规范来保证质量
